# This app stores the Groq API key in localStorage (set via /settings).
# No server-side env vars are needed.
# If you prefer server-side key injection during development, you can add:
# GROQ_API_KEY=your_key_here
# and read it from process.env in your API routes.
